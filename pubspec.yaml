name: movie_app_assignment
description: A new Flutter project.

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.0.5
  dio: ^5.4.0
  cached_network_image: ^3.3.1
  flutter_local_notifications: ^17.0.0
  percent_indicator: ^4.2.3
